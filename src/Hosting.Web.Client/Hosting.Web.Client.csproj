<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

	<PropertyGroup>
		<BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
		<NoDefaultLaunchSettingsFile>true</NoDefaultLaunchSettingsFile>
		<StaticWebAssetProjectMode>Default</StaticWebAssetProjectMode>
	</PropertyGroup>

	<ItemGroup>
		<NorseRef Include="Abstractions.Contracts">
			<Repo>Asgard</Repo>
		</NorseRef>
		<NorseRef Include="AuthN.Components.FluentUI">
			<Repo>Heimdall</Repo>
		</NorseRef>
		<NorseRef Include="Infrastructure.Components.Theme.FluentUI">
			<Repo>Midgard</Repo>
		</NorseRef>
		<NorseRef Include="Infrastructure.Web.Client" Generator="true">
			<Repo>Midgard</Repo>
		</NorseRef>
		<!-- Direct reference (CI-mode attachment) so NORSE080's bundled analyzer isn't stripped by
		     NuGet's default transitive exclude — it previously only reached this project through
		     Infrastructure.Web.Client, which doesn't survive the trip in standalone/package mode. -->
		<NorseRef Include="Infrastructure.Web.Grpc">
			<Repo>Midgard</Repo>
		</NorseRef>
		<!-- Dev-mode attachment (UseProjectReferences=true) — the NorseRef above covers CI/standalone. -->
		<NorseGeneratorRef Include="Infrastructure.Web.Grpc.Analyzers">
			<Repo>Midgard</Repo>
		</NorseGeneratorRef>
		<NorseRef Include="Reference.Contracts">
			<Repo>Mimir</Repo>
		</NorseRef>
		<PackageReference Include="Grpc.Net.Client" />
		<PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" />
		<PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.Authentication" />
		<!-- App.razor (Hosting.Web.Server) links _content/Norse.DesignSystem.Tokens/norse-design-tokens.css
		     directly, but no Yggdrasil project ever carried a PackageReference to the package that
		     static asset ships in — Infrastructure.Components.Theme.FluentUI pulls in
		     Norse.DesignSystem.Tokens's C# surface (FluentTokenSeed) transitively, but that doesn't
		     guarantee the CSS static web asset itself reaches a host's manifest, and in practice it
		     wasn't (the link 404s). The reference belongs here only, not on Hosting.Web.Server too:
		     Hosting.Web.Server already ProjectReferences this project, so it inherits the static web
		     asset transitively — a second direct reference there would be redundant. Direct reference
		     here, not NorseRef: Naglfar ships one generated .NET package with no realm submodule
		     dev/CI dual-mode story to forward, same as Midgard's own plain PackageReference to it in
		     Infrastructure.Components.Theme.csproj. -->
		<PackageReference Include="Norse.DesignSystem.Tokens" />
		<!-- Overrides the stale >= 2.4.8 transitive floor protobuf-net.Grpc 1.2.5 declares for
		     plain protobuf-net. -->
		<PackageReference Include="protobuf-net" />
		<PackageReference Include="protobuf-net.Grpc" />
		<ProjectReference Include="..\Hosting.Web.Components\Hosting.Web.Components.csproj" />
		<SupportedPlatform Include="browser" />
	</ItemGroup>

</Project>
