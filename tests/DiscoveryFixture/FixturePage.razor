@page "/discovery-fixture"

<h1>Discovery Fixture</h1>

<p>The one @@page-routed component this fixture carries — proves the generated discovery finds and includes a routable component's assembly in <c>RoutesAdditionalAssemblies</c>, not just a host's own.</p>
